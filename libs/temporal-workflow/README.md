# temporal-workflow

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build temporal-workflow` to build the library.

## Running unit tests

Run `nx test temporal-workflow` to execute the unit tests via [Jest](https://jestjs.io).
